---
import type { BlokContentParagraph } from '@libs/space-blok'

import ContentRichText from './ContentRichText.astro'

export type Props = {
  content: BlokContentParagraph
}

const { content } = Astro.props
---

<p><ContentRichText elements={content.elements} /></p>
