---
import type { BlokContentQuote } from '@libs/space-blok'

import ContentRichText from './ContentRichText.astro'

export type Props = {
  content: BlokContentQuote
}

const { content } = Astro.props
---

<q
  class="before:content-[''] after:content-[''] border-l-2 pl-2 border-not-pink"
>
  <ContentRichText elements={content.elements} />
</q>
